# Sarvam API key — required for live OCR of uploaded forms/documents.
# Get a key from https://sarvam.ai then restart uvicorn.
API_KEY=your_sarvam_api_key_here

# Demo desk login seeds (read at API start; hashed into local SQLite — never commit real values).
# DEMO_OPERATOR_PASSWORD=
# DEMO_ADMIN_PASSWORD=
